import comando_ver_objeto_IMP

def processa(ses, args):
  """Essa função retorna uma página HTML com as informações referentes ao objeto recebido como parâmetro."""
  return comando_ver_objeto_IMP.processa(ses, args)
