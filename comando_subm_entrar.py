import comando_subm_entrar_IMP

def processa(email, senha):
    """Esta função é chamada quando o usuário aperta o botão "Entrar"
    no formulário de login. Recebe o {email} e {senha} que o usuário preencheu
    nesse formulário.  Deve criar uma nova uma sessão para esse 
    usuário, com um novo cookie; e devolver a página de entrada."""
    return comando_subm_entrar_IMP.processa(email, senha)
