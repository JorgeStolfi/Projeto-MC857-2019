

Problemas no módulo `base_usuarios`:

Interface `base_usuarios.py`:

Implementação `base_usuarios_IMP.py`:

* As funções que estão na interface devem ser documentadas apenas na interface, não na implementação.
* A implementação não deve conectar com a base. Deve usar o módulo {base.py} e o argumento {bas} dado.
* Falta implementar várias funções da interface.
* As implementações que não podem ser completadas pro problemas em outros módulos devem devolver um valor do tipo correto (possivelmente {None}) 

Teste `testes/base_usuarios_TST.py`:

* Escrever este programa.  Deve chamar cada função ou módulo da interface, pelo menos uma vez, sem dar erro.
